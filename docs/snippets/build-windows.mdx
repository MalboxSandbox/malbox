Guest plugins that run inside a Windows analysis VM need to be cross-compiled. The Malbox Nix flake provides a MinGW toolchain for this.

<Note>
The Malbox Nix flake automatically configures the MinGW linker and library paths. No additional setup is needed if you're using `nix develop`.
</Note>
