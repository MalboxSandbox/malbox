Guest plugins that run inside a Windows analysis VM need to be cross-compiled. The Malbox devenv provides a MinGW toolchain for this.

<Note>
The Malbox devenv automatically configures the MinGW linker and library paths. No additional setup is needed if you're using `devenv shell`.
</Note>
