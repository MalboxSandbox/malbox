[package]
name = "malbox-plugin-internal"
version = "0.1.0"
edition = "2024"

[features]
default = []
host = []
plugin = []

[dependencies]
malbox-plugin-transport = { path = "../malbox-plugin-transport", features = ["ipc"] }

iceoryx2-loggers = { version = "0.8.1", default-features = false, features = ["std", "tracing"] }

tokio = { workspace = true }

dashmap = "6"
notify = "8.2.0"
arc-swap = "1.8.2"
semver.workspace = true
serde.workspace = true
toml.workspace = true
prost = "0.14.3"
tonic = "0.14.5"
tracing.workspace = true

color-eyre.workspace = true
thiserror.workspace = true

[dev-dependencies]
tempfile.workspace = true
tonic = "0.14.5"
tokio-stream = { workspace = true }
async-stream = "0.3"
